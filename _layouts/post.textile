{% include header.textile %}

h2. {{ page.title }}

<div class="post">

h2. {{ page.title }}

  <div class="published" data-epoch="{{ page.published.epoch }}">{{ page.published.utc }}</div>

  {% if page.tags != empty %}
  <div class="tags">{{ page.tags | array_to_sentence_string }}</div>
  {% endif %}

  <div class="body">
    {{ content }}
  </div>
</div>

{% include footer.textile %}
